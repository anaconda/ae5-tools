name: ae5-tools
commands:
  default:
    unix: python -m ae5_tools.k8s.server
  promql:
    unix: python -m ae5_tools.promql.server
channels:
  - defaults
packages:
  - python-dateutil
  - aiohttp
env_specs:
  default: {}
